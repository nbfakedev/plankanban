# Server
NODE_ENV=development
PORT=3000
BASE_URL=https://your-domain.tld

# Postgres
PGHOST=localhost
PGPORT=5432
PGDATABASE=kanban
PGUSER=kanban
PGPASSWORD=kanban_password
DATABASE_URL=postgres://kanban:kanban_password@localhost:5432/kanban

# Auth
JWT_SECRET=change_me
JWT_TTL_HOURS=24

# LLM gateway defaults
LLM_DEFAULT_PROVIDER=anthropic
LLM_DEFAULT_MODEL=claude-sonnet-4
LLM_RATE_LIMIT_PER_MINUTE=30
LLM_ALLOWED_MODELS_ANTHROPIC=claude-sonnet-4,claude-3-5-sonnet-latest
LLM_STUB_MODE=0

# Anthropic bridge (optional)
ANTHROPIC_API_KEY=change_me
CLOUDFLARE_WORKER_URL=https://your-worker.workers.dev
WORKER_SHARED_SECRET=change_me
